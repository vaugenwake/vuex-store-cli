/**
  * Populate store actions
  * Actions can be async as well
*/

export default {
    get_all(context, payload) {
        // Put your logic/async calls here
        // i.e. fetch(url, payload).then()
    }
}