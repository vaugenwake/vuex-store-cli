/**
 * Mutate the values in your store.
 * These are non async methods
 */

export default {
    UPDATE_DATA(state, payload) {
        // Update an item in your store
        // i.e. state.item = payload
    }
}